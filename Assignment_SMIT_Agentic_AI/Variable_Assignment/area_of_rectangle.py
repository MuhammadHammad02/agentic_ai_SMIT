# Objective: Logical math with variables. Task: Calculate the area of a rectangle using the formula: length * width.

if __name__ == "__main__":

    length = 10  # You can change this value to test with different lengths
    width = 5    # You can change this value to test with different widths

    area = length * width

    print(f"The area of the rectangle with length {length} and width {width} is {area}.")
    print("The area of the rectangle with length {} and width {} is {}.".format(length, width, area))   
    
